@page
@model ProjetoBase.Pages.ProdutosModel
@{
    ViewData["Title"] = "Produtos";
}

<h1>Lista de Produtos</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Preço</th>
            <th>Descrição</th>
        </tr>
    </thead>
    <tbody>
        @* TODO: percorra Model.Produtos com @foreach e exiba uma <tr> para cada produto *@
        @* Dentro da <tr>, crie quatro <td> com: produto.Id, produto.Nome, produto.Preco, produto.Descricao *@
    </tbody>
</table>
